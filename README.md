# User Dashboard Application by Ionic

Basic Crud operations are applied and using localstorage for storing the data and managing the changes.

## Steps to run it

- clone the project into new project folder created.
- install the dependencies using this command npm install
- run the project using 'ionic serve'

## Screenshots are attached below

- coming soon
